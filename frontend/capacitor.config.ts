import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'erasmust_frontend',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
